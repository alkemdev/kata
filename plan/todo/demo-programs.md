# TODO: Write intermediate demo programs

Current demos: `hello/` (trivial), `fib/` (small), `zoo/` (comprehensive but single-file).

Add demos that exercise specific feature combinations:
- **calculator** — parsing, Res error handling, match, string methods (once available)
- **linked list** — custom data structure with generics, Drop protocol, iteration
- **mini grep** — string processing, file-like iteration, Res for errors
- **state machine** — enums as states, match for transitions, methods for actions
- **matrix** — multi-arg indexing (`m[i, j]`), operator overloading (when available)
- **json parser** — recursive descent, enum AST, match, string methods
- **ecs** — entity-component-system pattern exercising generics, maps (when available), iteration

Each demo should be a self-contained `.ks` file with comments explaining the pattern.
